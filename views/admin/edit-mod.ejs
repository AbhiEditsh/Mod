<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Mod - ETS2 Mods</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>

<body>
    <div class="admin-container">
        <aside class="sidebar">
            <h2>Admin Panel</h2>
            <nav>
                <ul>
                    <li><a href="/admin/dashboard">Dashboard</a></li>
                    <li><a href="/admin/mods" class="active">Manage Mods</a></li>
                    <li><a href="/admin/mods/add">Add New Mod</a></li>
                    <li><a href="/admin/tutorials">Manage Tutorials</a></li>
                    <li><a href="/admin/logout">Logout</a></li>
                </ul>
            </nav>
        </aside>

        <main class="content">
            <div class="content-header">
                <h1>Edit Mod</h1>
                <a href="/admin/mods" class="btn-back">Back to Mods</a>
            </div>

            <div class="edit-form-container">
                <form action="/admin/mods/edit/<%= mod._id %>" method="POST" enctype="multipart/form-data"
                    class="mod-form">
                    <div class="form-group">
                        <label for="title">Title</label>
                        <input type="text" id="title" name="title" value="<%= mod.title %>" required>
                    </div>

                    <div class="form-group">
                        <label for="game">Game</label>
                        <select id="game" name="game" required onchange="updateCategories()">
                            <option value="">Select Game</option>
                            <option value="ETS2" <%=mod.game==='ETS2' ? 'selected' : '' %>>Euro Truck Simulator 2
                            </option>
                            <option value="Snowrunner" <%=mod.game==='Snowrunner' ? 'selected' : '' %>>Snowrunner
                            </option>
                            <option value="GTA5" <%=mod.game==='GTA5' ? 'selected' : '' %>>GTA 5</option>
                            <option value="GMod" <%=mod.game==='GMod' ? 'selected' : '' %>>Garry's Mod</option>
                            <option value="Melon" <%=mod.game==='Melon' ? 'selected' : '' %>>Melon Playground</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="category">Category</label>
                        <select id="category" name="category" required disabled>
                            <option value="">Select Category</option>
                            <option value="<%= mod.category %>" selected>
                                <%= mod.category %>
                            </option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" rows="5"
                            required><%= mod.description %></textarea>
                    </div>

                    <div class="form-group">
                        <label for="version">Version</label>
                        <input type="text" id="version" name="version" value="<%= mod.version %>" required>
                    </div>

                    <div class="form-group">
                        <label for="downloadUrl">Download URL</label>
                        <input type="url" id="downloadUrl" name="downloadUrl" value="<%= mod.downloadUrl %>" required>
                    </div>

                    <div class="form-group">
                        <label>Current Images</label>
                        <div class="current-images">
                            <% mod.images.forEach((image, index)=> { %>
                                <div class="image-preview">
                                    <img src="<%= image %>" alt="Mod image <%= index + 1 %>">
                                </div>
                                <% }) %>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="images">Add New Images</label>
                        <input type="file" id="images" name="images" multiple accept="image/*">
                        <small>Select multiple images to replace current ones (optional)</small>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn-primary">Update Mod</button>
                        <a href="/admin/mods" class="btn-cancel">Cancel</a>
                    </div>
                </form>
            </div>
        </main>
    </div>
</body>

</html>